# Publish Assets

The canonical marketplace asset directory is now `assets/marketplace/` at the repository root.

This directory is retained only as a legacy reference and should not be treated as the active export target.