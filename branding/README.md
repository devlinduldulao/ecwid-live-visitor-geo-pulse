# Branding Assets

Marketplace source artwork now lives in `assets/marketplace/` at the repository root.

This directory is retained only as a legacy reference and should not be treated as the canonical publication path.